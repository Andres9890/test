name: Run Sherlock

on:
  workflow_dispatch:
    inputs:
      username:
        description: 'Username to search with Sherlock'
        required: true
        type: string

jobs:
  sherlock-search:
    runs-on: ubuntu-latest

    steps:
      - name: Install pipx and Sherlock
        run: |
          python3 -m pip install --user pipx
          python3 -m pipx ensurepath
          pipx install sherlock-project

      - name: Run Sherlock
        run: |
          sherlock "${{ github.event.inputs.username }}" --print-found

      - name: Save & upload results
        uses: actions/upload-artifact@v4
        with:
          name: sherlock-results
          path: sherlock/results/*
